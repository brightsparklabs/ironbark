# RKE2 server configuration
# See: https://docs.rke2.io/install/configuration

# SELinux support:
# DO NOT enable 'selinux: true' unless the rke2-selinux policy package is installed.
# Without the policy, RKE2 will fail to start with: "process is not running in context 'container_runtime_t'".
# For air-gapped deployments, it's simpler to run SELinux in permissive mode (the default).
# Rook-Ceph will still work correctly via ROOK_HOSTPATH_REQUIRES_PRIVILEGED=true in the operator config.
# selinux: true

# Use the following to explicitly set the IP address of node.
# Default behaciour when unset is to derive from the default route.
#node-ip: 192.168.1.100
#advertise-address: 192.168.1.100

# Use Cilium as the CNI (Container Network Interface).
# Cilium provides eBPF-based networking, security, and observability.
cni: cilium

disable:
  # Disable RKE2's built-in ingress controller.
  # Use Cilium's ingress controller instead for better integration with the CNI.
  - rke2-ingress-nginx

# Do not set a default ingress controller as it is disabled above.
ingress-controller: none

# Disable kube-proxy to allow Cilium to handle service load balancing via eBPF.
# This enables Cilium's kube-proxy replacement feature for better performance.
disable-kube-proxy: true

# Make kubeconfig readable by non-root users.
write-kubeconfig-mode: "0644"

# Additional TLS SANs for API server certificate.
# Add any additional hostnames or IPs that clients will use to access the API server.
tls-san:
  - localhost
  - 127.0.0.1
  # - your-hostname.example.com
  # - 192.168.1.100
